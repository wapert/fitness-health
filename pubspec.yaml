name: fitness_health
description: 重訓・伸展・飲食營養 全方位健身 App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Navigation
  go_router: ^13.2.0

  # Local database
  drift: ^2.18.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.3
  path: ^1.9.0

  # Networking
  dio: ^5.4.3
  retrofit: ^4.1.0

  # UI
  flutter_animate: ^4.5.0
  cached_network_image: ^3.3.1
  video_player: ^2.8.6
  chewie: ^1.8.3          # video player UI wrapper
  fl_chart: ^0.68.0       # charts for nutrition/progress
  lottie: ^3.1.2          # animations

  # Body map (SVG)
  flutter_svg: ^2.0.10+1

  # Fasting timer
  flutter_local_notifications: ^17.1.2

  # Food / nutrition
  http: ^1.2.1

  # URL launcher (open YouTube videos in browser)
  url_launcher: ^6.3.2

  # Utilities
  intl: ^0.19.0
  shared_preferences: ^2.2.3
  uuid: ^4.4.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  audioplayers: ^6.7.0  # kept for potential future use
  just_audio: ^0.10.5
  table_calendar: ^3.1.3

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  freezed: ^2.5.2
  json_serializable: ^6.8.0
  riverpod_generator: ^2.4.0
  drift_dev: ^2.18.0
  retrofit_generator: ^8.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/animations/
    - assets/audio/
